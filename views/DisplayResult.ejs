<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Result</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900">
    <div class="w-full h-full p-10 text-white">
        <div class="nav mb-10">
            <a class="text-blue-500" href="/result">Go Back</a>
        </div>

        <h1 class="text-3xl tracking-tighter mb-3">Result for <%= student.Fname %> <%= student.Lname %></h1>
        
        <!-- Check if student results exist -->
        <% if (student.subject && student.semester && student.marks) { %>
            <table class="w-full text-left bg-zinc-800 border border-zinc-700 rounded-lg">
                <thead>
                    <tr class="border-b border-zinc-700">
                        <th class="px-5 py-3">Subject</th>
                        <th class="px-5 py-3">Semester</th>
                        <th class="px-5 py-3">Marks</th>
                    </tr>
                </thead>
                <tbody>
                    <tr class="border-b border-zinc-700">
                        <td class="px-5 py-3"><%= student.subject %></td>
                        <td class="px-5 py-3"><%= student.semester %></td>
                        <td class="px-5 py-3"><%= student.marks %></td>
                    </tr>
                </tbody>
            </table>
        <% } else { %>
            <p>No result found for this student.</p>
        <% } %>
    </div>
</body>
</html>
